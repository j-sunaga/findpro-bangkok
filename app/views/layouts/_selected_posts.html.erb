<tbody>
  <% @posts.each do | post | %>
  <tr>
    <td><%= link_to "#{post.title}", post %></td>
    <td><%= post.recruiter.name %></td>
    <td><%= post.selected_user.name %></td>
    <td><%= post.status %></td>
  </tr>
  <% end %>
</tbody>
